# Phase 9.5 Challenge

1. 在会话列表增加纯客户端标题过滤，不改变 Java API；解释为什么筛选结果不是新的事实来源。
2. 给 `MessageList` 增加来源数量徽标，需要先评估当前消息查询 DTO 是否包含 sources，禁止伪造。
3. 为 429 展示 `Retry-After` 倒计时，但仍禁止自动重新发送聊天请求。
4. 编写一个路由测试：无 Token 访问 `/chat` 跳转登录，401 后不能通过后退键恢复可信身份。

每个练习都必须重新运行类型检查和相关测试；若需要修改公共 DTO 或后端契约，先停止并走契约
变更 Review。
