# Insurance AI Agent - default runtime dependencies
# Streamlit + LangGraph + LangChain RAG (see separate lists for LlamaIndex and training)

# --- LangGraph core ---
langgraph>=1.0.0,<2.0.0
langgraph-checkpoint>=4.0.0,<5.0.0

# --- LangChain ecosystem ---
langchain-core>=1.0.0,<2.0.0
langchain-openai>=1.0.0,<2.0.0
langchain-community>=0.4.0,<0.5.0
langchain-text-splitters>=1.0.0,<2.0.0

# --- Embedding ---
sentence-transformers>=3.0.0,<6.0.0
# Keep explicit transitive runtime bounds for sentence-transformers.
# Transformers 5 and Torch 2.8+ have not passed this project's Windows validation.
transformers>=4.41.0,<5.0.0
torch>=2.4.0,<2.8.0

# --- Vector database ---
faiss-cpu>=1.8.0,<2.0.0

# --- PDF processing ---
PyMuPDF>=1.24.0,<2.0.0

# --- Web interfaces ---
streamlit>=1.35.0,<2.0.0
fastapi>=0.115.0,<1.0.0
uvicorn>=0.30.0,<1.0.0
httpx>=0.27.0,<1.0.0
python-multipart>=0.0.9,<1.0.0

# --- Environment variables ---
python-dotenv>=1.0.0,<2.0.0

# --- Data models ---
pydantic>=2.0.0,<3.0.0
